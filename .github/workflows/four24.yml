name: four24
on: workflow_dispatch
permissions:
  id-token: write
  contents: read
  attestations: write
jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 4 > answer.txt
      - uses: actions/attest-build-provenance@v2
        with:
          subject-path: answer.txt
